<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zz.mapper.StudentMapper">
    <select id="queryStudentByStuId" resultType="Student">
        select *
        from student
        where student_id = #{stuId};
    </select>
    <select id="queryStudentRecordByPage" resultType="Student">
        select * from student where 1=1
        <if test="name != null and name !=''">
            and name like #{name}
        </if>
        limit #{page},#{limit}
    </select>

    <select id="countStudentRecord" resultType="Student">
        select count(*) counts from student where 1=1
        <if test="name != null and name !=''">
            and name like #{name}
        </if>
    </select>

    <insert id="addStudent" parameterType="Student">
        insert into student(name, sex, age, student_id, counselor_work_id)
        values (#{student.name}, #{student.sex}, #{student.age}, #{student.studentId}, #{student.counselorWorkId})
    </insert>
    <select id="queryStudentRecordById" resultType="Student">
        select *
        from student
        where id = #{id}
    </select>
</mapper>